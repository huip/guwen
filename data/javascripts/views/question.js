// Generated by CoffeeScript 1.6.3
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(function(require, exports, module) {
  var $, Backbone, QuestionModel, QuestionView, _, _ref;
  $ = require('$');
  Backbone = require('backbone');
  _ = require('underscore');
  QuestionModel = require('../models/question');
  QuestionView = (function(_super) {
    __extends(QuestionView, _super);

    function QuestionView() {
      _ref = QuestionView.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    QuestionView.prototype.initialize = function() {
      var questionModel, that;
      that = this;
      questionModel = new QuestionModel({
        id: this.id
      });
      return questionModel.fetch({
        success: function(data) {
          return that.render(data.toJSON());
        }
      });
    };

    QuestionView.prototype.render = function(data) {
      var template;
      template = _.template($('#question_template').html(), {
        data: data
      });
      return this.$el.html(template);
    };

    return QuestionView;

  })(Backbone.View);
  return module.exports = QuestionView;
});
